import { createTheme } from "@mui/material";

export const primaryLightTheme = createTheme({

    typography:{
        h1: {
        },
        button: {
            textTransform: 'none',
        },
    },
    
    
  });